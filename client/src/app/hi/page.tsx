import { Button } from "@mui/material";
import React from "react";

type Props = {};

const HiPage = (props: Props) => {
  return (
      <Button href='/'>HiPage</Button>
  );
};

export default HiPage;
