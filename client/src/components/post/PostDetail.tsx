import { PostInterface } from "@/types/post/PostInterface";

const PostDetail = ({
  image,
  createdAt,
  userId,
  nickname,
  content,
  userImage,
  tags,
  id,
}: PostInterface) => {
  return <>{userId}</>;
};
export default PostDetail;
